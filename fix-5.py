# ============================================================
# GLOBAL NO SURE BET FILTER (CRITICAL)
# ============================================================

if r["confidence"] >= 0.80:
    stava = "NO BET"
    razlog = "OVERCONFIDENCE BLOCK"